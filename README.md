## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Create `.env.local` and set `VITE_FIREBASE_API_KEY` to enable Firebase auth. The rest of the Firebase configuration lives in `firebase-applet-config.json`; the API key is intentionally kept out of source control.
3. Run the app:
   `npm run dev`
